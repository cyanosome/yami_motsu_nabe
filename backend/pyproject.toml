[project]
name = "backend"
version = "0.1.0"
description = "Yami Motsu Nabe Backend API with WebSocket & Redis support"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "fastapi[standard]>=0.137.0,<0.138.0",
    "pydantic>=2.13.0,<3.0.0",
    "redis>=5.0.0",
    "websockets>=12.0",
]
